package es.rmc.controller;

/** Default home (root) controller (redirect to swagger api docs)
 * 
 * @author rmc */
public interface RootController {
	
	/** manages default url for the microservice  */
	String index();
}
